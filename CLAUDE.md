# إرشادات المستودع

## طبيعة المشروع

موقع ثابت بالكامل (HTML/CSS/JS بلا خادم) يُنشر عبر GitHub Pages من فرع
`main` والجذر `/`. لا توجد خطوة بناء ولا مدير حزم — الملفات تُقدَّم كما هي.
البيانات تُحفظ في `localStorage` داخل متصفح المستخدم.

عند تعديل ملفات `assets/css` أو `assets/js` حدّث رقم النسخة في وسم
الاستدعاء (`?v=`) لكسر ذاكرة التخزين المؤقت.

## البنية

| الملف | المحتوى |
|---|---|
| `index.html` | لوحة القيادة |
| `health.html` | القسم الصحي |
| `finance.html` | القسم المالي |
| `matjarlink.html` | مشروع متجرلينك |
| `life.html` | الحياة |
| `settings.html` | الإعدادات والنسخ الاحتياطي |
| `HMT_Tasks.html` | خطة التنفيذ |

## أدوات التصفح

`.claude/tools/browser/` فيه أداة لفتح صفحات الويب في متصفح Chromium حقيقي
والتقاط لقطات شاشة منها، وهي لازمة لأن بيئة التشغيل السحابية تمنع Chromium
من فتح أنفاق HTTPS مباشرة.

```bash
node .claude/tools/browser/browse.js <url> [--shot out.png] [--json]
```

التفاصيل والحدود في `.claude/tools/browser/README.md`.

للاطلاع على محتوى صفحة نصياً فقط، `WebFetch` و`WebSearch` أسرع وأوثق —
لا تستخدم المتصفح إلا عند الحاجة إلى العرض المرئي أو لقطة الشاشة.

مجلد `.claude/` لا يُنشر على GitHub Pages (المجلدات التي تبدأ بنقطة مستثناة).
