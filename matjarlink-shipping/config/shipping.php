<?php

declare(strict_types=1);

use App\Domains\Shipping\Integration\Drivers\Manual\ManualDriver;

return [

    /*
    |--------------------------------------------------------------------------
    | تعريف الـ Drivers
    |--------------------------------------------------------------------------
    | إضافة شركة شحن جديدة = سطر واحد هنا + صنف يحقق عقد CarrierDriver.
    | لا تُعدَّل النواة ولا قاعدة البيانات ولا الواجهات.
    */
    'drivers' => [
        'manual' => ManualDriver::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | المهلات (بالثواني)
    |--------------------------------------------------------------------------
    | التسعير قصير المهلة عمداً: شركة بطيئة يجب ألا توقف عرض بقية الأسعار.
    */
    'timeouts' => [
        'rate'   => 5,
        'create' => 15,
        'label'  => 20,
        'track'  => 10,
        'test'   => 8,
    ],

    /*
    |--------------------------------------------------------------------------
    | قاطع الدائرة (Circuit Breaker) لكل حساب شركة
    |--------------------------------------------------------------------------
    */
    'circuit_breaker' => [
        'failure_threshold' => 5,
        'open_seconds'      => 300,
    ],

    /*
    |--------------------------------------------------------------------------
    | أوزان التوصية في مقارنة الأسعار
    |--------------------------------------------------------------------------
    | تاجر يريد الأرخص وآخر يريد الأسرع — الأوزان قابلة للضبط لكل متجر.
    */
    'scoring' => [
        'price'       => 0.30,
        'speed'       => 0.20,
        'reliability' => 0.30,
        'punctuality' => 0.20,
    ],

    /*
    |--------------------------------------------------------------------------
    | الوزن والأبعاد
    |--------------------------------------------------------------------------
    */
    'weight' => [
        'unit'                => 'kg',
        'dimension_unit'      => 'cm',
        'volumetric_divisor'  => 5000,
        'max_kg'              => 30,
    ],

    /*
    |--------------------------------------------------------------------------
    | المزامنة التكيّفية: كل حالة ووتيرة استعلامها (بالدقائق)
    |--------------------------------------------------------------------------
    */
    'sync_intervals' => [
        'created'          => 360,
        'picked_up'        => 180,
        'in_transit'       => 180,
        'out_for_delivery' => 30,
        'failed_attempt'   => 60,
        'exception'        => 60,
        'webhook_backstop' => 1440,
    ],

    'stale_after_hours' => 72,

    /*
    |--------------------------------------------------------------------------
    | التخزين
    |--------------------------------------------------------------------------
    | قرص خاص إلزاماً — البوالص تحمل بيانات عملاء ولا تُنشر أبداً.
    */
    'storage' => [
        'disk'              => env('SHIPPING_DISK', 'local'),
        'labels_path'       => 'shipping/labels',
        'signed_url_minutes' => 15,
    ],

    'reference_prefix' => 'SHP',

    'currency' => env('SHIPPING_CURRENCY', 'OMR'),
    'decimals' => 3,

    'queues' => [
        'critical' => 'shipping-critical',
        'webhooks' => 'shipping-webhooks',
        'sync'     => 'shipping-sync',
        'labels'   => 'shipping-labels',
        'bulk'     => 'shipping-bulk',
    ],

    'logs' => [
        'retention_days'         => 90,
        'failed_retention_days'  => 180,
        'redact'                 => ['api_key', 'password', 'secret', 'token', 'authorization', 'client_secret'],
    ],

    'routes' => [
        'prefix'     => 'shipping',
        'middleware' => ['web', 'auth'],
    ],
];
